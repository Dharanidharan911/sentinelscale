apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: sentinelscale
  labels:
    app.kubernetes.io/name: prometheus
    app.kubernetes.io/part-of: sentinelscale
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: prometheus
  ports:
    - name: http
      port: 9090
      targetPort: 9090
